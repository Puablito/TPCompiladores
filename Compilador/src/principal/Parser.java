package principal;

public class Parser {
	
/*	private Lexico analizadorLexico;
	
	public Parser(analizadorLexico) {
		this.analizadorLexico = analizadorLexico;
		token = this.analizadorLexico.getToken();
		while (token != -1){
			token = this.analizadorLexico.getToken();
			System.out.println(token);
		}
	this.analizadorLexico.mostrarTablaSimbolos();
	}
*/
}
